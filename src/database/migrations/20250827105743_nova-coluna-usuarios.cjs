/**
 * Migration placeholder para corresponder à entrada existente em knex_migrations.
 * Não faz alterações (no-op) — usado apenas para restaurar consistência entre DB e arquivos.
 */

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function(knex) {
  // placeholder: nenhuma alteração necessária
  return Promise.resolve();
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function(knex) {
  // placeholder
  return Promise.resolve();
};
